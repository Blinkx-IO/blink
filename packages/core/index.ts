//TODO add a fetch method to call the various api's ie content, contacts, commerce and more

//TODO add an init method to set up api key

//TODO add components section for the editor, methods required are addComponents - string, or JSON obj, and CSS classes via addRules 
import Blink from "./src/blink.js";

const blink = new Blink();

export {blink};

